class Inventory {
    constructor () {
        this.items = {};
    }
    useItem (itemName) {
        // find item in this.items object, get reference to item, use item specific use
        // function
    }
    dropItem (itemName) {

    }
    giveItem (itemName) {
        
    }
}

export default Inventory;
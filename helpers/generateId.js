const generateId = (type) => {
  const id = null;
  // simple demo logic: switch based on string type passed in
  switch (type) {
    case 'game':
      break;
    case 'player':
      break;
    default:
  }
  // todo
  return id;
};

module.exports = generateId;
